/**
 * Main entry point for analytics services
 * Why: Provides a clean interface for importing analytics functionality
 * and abstracts the internal structure from consumers
 */
export { AnalyticsService as analyticsService } from './services/AnalyticsService';
